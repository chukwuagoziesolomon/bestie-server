{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install -r requirements.txt",
    "startCommand": "gunicorn scribbleintimeai.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
